# v1.3.0 Hotfix 1

- Fixed 8-bit PS2 -> 4-bit PS2 color-depth conversion for valid rectangular textures such as 256x128.
- Removed the old square-only pre-check from the 4-bit PS2 interlace converter.
- Safety is still enforced by the converter's bounds and full-pixel-coverage validation before writing.
- Verified against the real `r100_04.tpl`: its 4-bit PS2 256x128 textures at indices 22 and 50 round-trip PS2 -> linear -> PS2 byte-for-byte.
