# RE4 PS2 TPL Manager v1.3.1

Maintenance release.

## Removed

- **Fix broken TPL (experimental)** menu command and its dedicated UI handler.
- **Fix transparency** menu command and its dedicated UI handler.
- The now-unused trailing separator from the Tools menu.

The internal offset rebuild service remains because it is still used by normal TPL editing operations.
